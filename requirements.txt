# Silent Patient Pool Finder — Python dependencies
# Install with: pip install -r requirements.txt

# Core data
pandas>=2.1
numpy>=1.26
pyarrow>=14.0       # parquet support
pyyaml>=6.0
openpyxl>=3.1       # SAIPE XLS parsing
certifi>=2024.0     # CA bundle — fixes LibreSSL SSL issues on macOS
requests>=2.31      # HTTP client

# Feature engineering
scikit-learn>=1.4

# ML model (legacy pipeline: run.py → src/model/train.py + src/model/score.py)
# Not used by the real-data pipeline (ingest_real_data.py + dimension_scorer.py).
# Keep if running the synthetic XGBoost pipeline; safe to remove otherwise.
xgboost>=2.0
shap>=0.44
joblib>=1.3

# Database (Neon / PostgreSQL)
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Dashboard
streamlit>=1.32
plotly>=5.18

# Dev / testing
pytest>=8.0
